const spans = document.querySelectorAll(".progHtml span");

spans.forEach((span) => {
  span.style.width = span.dataset.width;
  span.innerHTML.widht = span.dataset.width;
});
const spans2 = document.querySelectorAll(".progCss span");

spans2.forEach((span) => {
  span.style.width = span.dataset.width;
  span.innerHTML.widht = span.dataset.width;
});
const spans3 = document.querySelectorAll(".progJs span");

spans3.forEach((span) => {
  span.style.width = span.dataset.width;
  span.innerHTML.widht = span.dataset.width;
});
